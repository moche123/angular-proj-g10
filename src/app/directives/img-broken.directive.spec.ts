
xdescribe('ImgBrokenDirective', () => {
  it('should create an instance', () => {
    // const directive = new ImgBrokenDirective(new ElementRef(null),new Renderer2());
    expect(1).toBe(1);
  });
});
